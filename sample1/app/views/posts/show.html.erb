<div class="main posts-show">
  <div class="container">
    <div class="posts-show-item">
      <p>
        ●<%= @post.title %>
      </p>
      <p>
        <%= @post.content %>
      </p>

      <div class="post-time">
        <%= l(@post.created_at.in_time_zone('Tokyo'), format: :long) %>
      </div>
      <div class="post-menus">
        <%= link_to("編集", "/posts/#{@post.id}/edit") %>
        <%= link_to("削除", "/posts/#{@post.id}/destroy", {method: "post"}) %>
      </div>
    </div>
  </div>
</div>
